/**
 * TODO: two simultaneous create_booking calls for a table with 3 seats left;
 * assert exactly one succeeds (row lock + derived remaining seats).
 *
 * TODO: pick a test runner (e.g. vitest) and hit the create_booking RPC.
 */

export {};
